import SwiftUI

struct Critical_care_view: View {
    var body: some View {
        VStack {
            Text("CriticalCareWatch")
                .font(.largeTitle)
                .padding()
            
            // Add your view content here
        }
        .navigationTitle("CriticalCareWatch")
    }
}

struct Critical_care_view_Previews: PreviewProvider {
    static var previews: some View {
        Critical_care_view()
    }
}


