import SwiftUI
struct Search_view: View {
    var body: some View {
        VStack {
            Text("Search Page")
            Spacer()
        }
        .navigationTitle("Search")
        .navigationBarTitleDisplayMode(.inline)
    }
}
